
If removing sd while recording. Device doesn't recover and require reset.

Find a safe way to do continious RX (dma?)

Merge the ADC and controller code that report log packets.

Handle ERROR_HANDLER errors more gracefully, printing string and file name/line before hanging.


-------

why defining PD4 as pullup instead of pulldown causes sd file open to fail?

Revisit the error handling in the serial and CDC logger drivers. Should we send to a trap?


